{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "@territorio/tsconfig/react-library",
  "_comment": "For packages that ship React components (packages/ui). DOM libs only \u2014 a shared component must not reach for Node globals.",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": []
  }
}
